csrf:
  csrf-enabled: ${csrf.csrf-enabled}
  cookie-name: XSRF-TOKEN
  header-name: X-XSRF-TOKEN
  allowed-methods:
    - HEAD
    - TRACE
    - OPTIONS
  ignored-endpoints:
    - pattern: /actuator/**